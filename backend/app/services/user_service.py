# user_service.py — User Profile Business Logic
#
# Functions to build:
#
# 1. get_user_by_id(user_id) — Fetch user from MongoDB by _id
# 2. get_user_by_email(email) — Fetch user by email
# 3. update_user(user_id, update_data) — Update user profile fields
# 4. delete_user(user_id) — Delete or soft-delete user account
# 5. get_public_profile(user_id) — Return only public-safe fields
